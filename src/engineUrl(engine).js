/*TODO setup url parsing to enable this
function engineUrl(engine) {
const engineUrlEngine = `${OPEN_AI_URL}/engines/${engine}`;
return engineUrlEngine;
}

function engine(engine) {
  const url = config.engineUrl(engine);
  return this.send_request(url, 'get');
}
*/