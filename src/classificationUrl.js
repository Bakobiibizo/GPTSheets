/*TODO setup url parsing to enable this
function classificationsUrl() {
const classificationsUrl = `${OPEN_AI_URL}/classifications`;
return classificationsUrl
}

function classification(opts) {
  const url = config.classificationsUrl();
  return this.send_request(url, "post", opts);
}
*/