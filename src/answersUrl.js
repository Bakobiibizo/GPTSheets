/*TODO setup url parsing to enable this
function answerUrl() {
  const answerUrl = `${OPEN_AI_URL}/answers`;
  return answerUrl;
}

function answers(opts) {
  const url = config.answersUrl();
  return this.send_request(url, 'post', opts);
}*/