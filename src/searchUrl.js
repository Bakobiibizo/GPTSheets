/*TODO setup url parsing to enable this
function searchUrl(engine) {
  if (engine == none) {
  engine = DEFAULT_ENGINE;
  }
const searchUrl = `${OPEN_AI_URL}/engines/${engine}/search`;
return searchUrl;
}


function search(opts) {
  const url = config.searchURL(opts.engine || DEFAULT_ENGINE);
  delete opts.engine;
  return this.send_request(url, 'post', opts);
}*/